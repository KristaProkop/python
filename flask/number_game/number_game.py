import random # import the random module
# The random module has many useful functions. This is one that gives a random number in a range
random.randrange(0, 101) # random number between 0-100# post data from form

# add it to session
# if 

# # Set session like so:
# session['someKey'] = 50
# # Remove something from session like so:
# session.pop('someKey')